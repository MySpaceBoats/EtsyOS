# Printify > My Profile > Connections > Generate personal access token
PRINTIFY_API_KEY=
# Optional — if unset, the server auto-selects the account's first shop
PRINTIFY_SHOP_ID=
# Optional — only needed for the upstream server's AI image-generation tools (out of Phase 1 scope)
# REPLICATE_API_TOKEN=
# IMGBB_API_KEY=
