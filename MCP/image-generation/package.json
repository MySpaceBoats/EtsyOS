{
  "name": "@etsyos/mcp-image-generation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EtsyOS image-generation MCP server — multi-provider image generation with automatic failover (Workers AI, Imagen, HuggingFace)",
  "scripts": {
    "start": "node --env-file=.env --experimental-strip-types src/run.ts",
    "typecheck": "tsc -p . --noEmit",
    "test": "node --experimental-strip-types --test \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.5"
  }
}
