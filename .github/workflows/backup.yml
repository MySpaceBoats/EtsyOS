name: backup
# ponytail: squelette documente — logique metier ajoutee a l'implementation de chaque routine.
# Couvre: Backup
on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — not yet implemented
        run: echo "EtsyOS workflow 'backup' — implementation pending. See /Routines and ROADMAP.md"
