name: publish
# ponytail: squelette documente — logique metier ajoutee a l'implementation de chaque routine.
# Couvre: Publishing, Printify-Sync
on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — not yet implemented
        run: echo "EtsyOS workflow 'publish' — implementation pending. See /Routines and ROADMAP.md"
