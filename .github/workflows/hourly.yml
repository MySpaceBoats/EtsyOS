name: hourly
# ponytail: squelette documente — logique metier ajoutee a l'implementation de chaque routine.
# Couvre: Market-Scan, Printify-Sync, Health-Check
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — not yet implemented
        run: echo "EtsyOS workflow 'hourly' — implementation pending. See /Routines and ROADMAP.md"
