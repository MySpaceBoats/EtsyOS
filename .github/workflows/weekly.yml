name: weekly
# ponytail: squelette documente — logique metier ajoutee a l'implementation de chaque routine.
# Couvre: Weekly-Review, Learning, Cleanup
on:
  schedule:
    - cron: '0 21 * * 0'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — not yet implemented
        run: echo "EtsyOS workflow 'weekly' — implementation pending. See /Routines and ROADMAP.md"
